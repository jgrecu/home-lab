---
apiVersion: volsync.backube/v1alpha1
kind: ReplicationSource
metadata:
  name: home-assistant-config-backup-r2
spec:
  sourcePVC: home-assistant
  trigger:
    schedule: "0 12 * * *"  # Daily at noon (offsite R2 backup)
  restic:
    repository: home-assistant-config-r2-restic-secret
    retain:
      daily: 7
      weekly: 4
      monthly: 12
    copyMethod: Snapshot
    storageClassName: longhorn-volsync
    volumeSnapshotClassName: longhorn
    capacity: 5Gi
    accessModes:
      - ReadWriteOnce
    pruneIntervalDays: 14
    cacheCapacity: 1Gi
    cacheStorageClassName: longhorn-single
    cacheAccessModes:
      - ReadWriteOnce
    moverResources:
      limits:
        memory: 512Mi
      requests:
        cpu: 50m
        memory: 256Mi
    moverSecurityContext:
      runAsNonRoot: true
      runAsUser: 65534
      fsGroup: 65534
      seccompProfile:
        type: RuntimeDefault
