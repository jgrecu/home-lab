---
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: cloudflare-dns
spec:
  interval: 15m
  layerSelector:
    mediaType: application/vnd.cncf.helm.chart.content.v1.tar+gzip
    operation: copy
  ref:
    tag: 1.21.1  # Pinned: v1.22.0+ breaks with Cloudflare Tunnel (tries to create A records with private IPs)
  url: oci://ghcr.io/home-operations/charts-mirror/external-dns
