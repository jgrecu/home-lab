---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: forgejo

components:
  - ../../components/sops

resources:
  - ./namespace.yaml
  - ./forgejo/ks.yaml
  - ./forgejo/backup-ks.yaml
  # forgejo-runner paused - requires eleboucher's runner fork (not upstream)
  # See docs/DEFERRED.md for current blocker and resolution options
  # - ./forgejo-runner/ks.yaml
