---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: forgejo

components:
  - ../../components/sops

resources:
  - ./namespace.yaml
  - ./forgejo/ks.yaml
  - ./forgejo/backup-ks.yaml
  # Forgejo runner disabled - requires Docker socket even for Kubernetes executor
  # See docs/DEFERRED.md for details and review schedule
  # - ./forgejo-runner/ks.yaml
